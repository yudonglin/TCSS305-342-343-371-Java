a. Time Complexity of encode(), decode() and equal() methods in terms of Big-O notation.
The Big-O of encode(), decode() and equal() methods are O(n)

b. What are the advantages and disadvantages of RLE? What are the applications of RLE?
RLE can reduce the size of a String if multiple letters appears at the same time.
However, as the example 3 from test 1 show, RLE is not good at reducing the size of a complete sentence.

c. Amount of time you spent on implementing the assignment? Challenges you faced while implementing the assignment? How did you overcome these challenges?
I spent 5 to 10 hours implementing the assignment. I do face some small challenges from small things, such as how to read and write to a row text file.
I overcome these challenges by doing reach and go through the document provided by Oracle.